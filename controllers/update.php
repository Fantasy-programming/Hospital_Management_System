<?php

use Core\App;
use Core\Database;

$db = App::resolve(Database::class);

// Create the query

// Check that it is the right user

// update the profile of the user with that data

// send back a success message JS (?)